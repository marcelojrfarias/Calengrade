{
  "name": "calengrade",
  "version": "0.1.0",
  "dependencies": {
    "concurrently": "^5.0.2"
  },
  "scripts": {
    "start": "yarn start --prefix backend",
    "dev": "concurrently \"yarn run backend-dev\" \"yarn run frontend-start\"",
    "heroku-postbuild": "yarn install --prefix backend && yarn install --prefix frontend && yarn build --prefix frontend"
  }
}
